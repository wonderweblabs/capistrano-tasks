require 'spec_helper'

describe Wwl::Capistrano::Tasks do
  it 'has a version number' do
    expect(Wwl::Capistrano::Tasks::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
